# llm-summarizer-app

update .env file and add variables on frontend

```env
REACT_APP_API_URL=http://localhost:3001
```

update .env file and add variables on backend

```env
OPENAI_API_KEY=sk-proj-xxx
PORT=3001
NODE_ENV=development
```

to run the app run

```js
docker-compose up --build
```
